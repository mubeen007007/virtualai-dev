<div class="feyre-tryon-wrap">
  <button
    type="button"
    class="feyre-tryon-trigger"
    onclick="feyreOpenTryOnDrawer()">
    Try It On
  </button>
</div>

<div id="feyre-tryon-overlay" class="feyre-tryon-overlay" style="display:none;" onclick="feyreOverlayClose(event)">
  <div id="feyre-tryon-drawer" class="feyre-tryon-drawer">
    <button type="button" class="feyre-close-btn" onclick="feyreCloseTryOnDrawer()">×</button>

    <div class="feyre-drawer-head">
      <h2>Try It On</h2>
      <div id="feyre-tryon-counter" class="feyre-tryon-counter">
        <span class="feyre-counter-icon">◷</span>
        <span id="feyre-counter-text">0 Try-Ons</span>
      </div>
    </div>

    <!-- STEP 1 -->
    <div id="feyre-step-1" class="feyre-step">
      <div class="feyre-step-progress">
        <div class="feyre-step-circle active">1</div>
        <div class="feyre-step-line"></div>
        <div class="feyre-step-circle">2</div>
      </div>

      <div class="feyre-step-title">Want to see it on you?</div>
      <div class="feyre-step-subtitle">
        Upload a full-body photo to visualize the fit on yourself.
      </div>

      <div class="feyre-product-preview-card">
        {% if product.featured_image %}
          <img
            class="feyre-product-preview-img"
            src="{{ product.featured_image | image_url: width: 700 }}"
            alt="{{ product.title | escape }}"
            width="220"
            height="320">
        {% endif %}
      </div>

      <input
        type="file"
        id="feyre-tryon-upload"
        accept="image/*"
        style="display:none;"
        onchange="feyreHandlePhotoSelect(event)">

      <button type="button" class="feyre-main-btn" onclick="document.getElementById('feyre-tryon-upload').click()">
        + Choose Your Photo
      </button>

      <button type="button" class="feyre-secondary-btn-lite">
        📷 Open Camera
      </button>

      <div class="feyre-footer-note">
        By using this service, you agree to our
        <a href="#" onclick="return false;">Terms & Privacy Policy</a>.<br>
        Photos are used solely for try-on. AI results may vary.
      </div>
    </div>

    <!-- STEP 2 -->
    <div id="feyre-step-2" class="feyre-step" style="display:none;">
      <div class="feyre-step-title review">Review Your Photo</div>

      <div class="feyre-review-card">
        <img
          id="feyre-review-image"
          class="feyre-review-image"
          src=""
          alt="Review photo"
          width="240"
          height="360">

        <button type="button" class="feyre-change-photo-btn" onclick="document.getElementById('feyre-tryon-upload').click()">
          📷 Change Photo
        </button>
      </div>

      <button type="button" id="feyre-generate-btn" class="feyre-main-btn" onclick="feyreGenerateTryOn()">
        Generate My Look
      </button>

      <div class="feyre-footer-note compact">
        By generating a look, you agree to our
        <a href="#" onclick="return false;">Terms & Privacy Policy</a>.
      </div>

      <div class="feyre-powered">Powered by <span>FEYRE TryOn</span></div>
    </div>

    <!-- STEP 3 -->
    <div id="feyre-step-3" class="feyre-step" style="display:none;">
      <div class="feyre-step-title generating">Generating your fit</div>

      <div class="feyre-loading-card">
        <div class="feyre-loading-frame">
          <div class="feyre-scan-line"></div>
          <img
            id="feyre-loading-image"
            class="feyre-loading-image"
            src=""
            alt="Loading">
        </div>
      </div>

      <div class="feyre-progress-wrap">
        <div class="feyre-progress-bar">
          <div id="feyre-progress-fill" class="feyre-progress-fill"></div>
        </div>
        <div id="feyre-progress-label" class="feyre-progress-label">12% Complete</div>
        <div id="feyre-progress-status" class="feyre-progress-status">Analyzing Your Photo....</div>
      </div>

      <div id="feyre-loading-note" class="feyre-loading-note">
        Our AI adjusts for fabric stretch and drape on your body.
      </div>

      <div class="feyre-powered loading-powered">Powered by <span>FEYRE TryOn</span></div>
    </div>

    <!-- STEP 4 -->
    <div id="feyre-step-4" class="feyre-step" style="display:none;">
      <div class="feyre-result-head">
        <button type="button" class="feyre-back-btn" onclick="feyreBackToReview()">‹</button>
        <div class="feyre-result-title">Here Is Your Fit</div>
        <div class="feyre-tryon-counter">
          <span class="feyre-counter-icon">◷</span>
          <span id="feyre-counter-text-final">1 Try-On</span>
        </div>
      </div>

      <div class="feyre-result-card">
        <img
          id="feyre-result-image"
          class="feyre-result-image"
          src=""
          alt="Try-on result"
          width="260"
          height="420">
      </div>

      <button type="button" class="feyre-main-btn result-cart-btn">
        👜 Add To Cart — {{ product.price | money }}
      </button>

      <div class="feyre-result-actions">
        <button type="button" class="feyre-action-btn">⇪ Share</button>
        <button type="button" class="feyre-action-btn" onclick="feyreDownloadResult()">⇩ Save</button>
        <button type="button" class="feyre-action-btn" onclick="feyreCloseTryOnDrawer()">↗ View Product</button>
      </div>
    </div>

    <div id="feyre-error-box" class="feyre-error-box" style="display:none;">
      <div class="feyre-error-title">Something went wrong</div>
      <pre id="feyre-error-text"></pre>
    </div>
  </div>
</div>

<style>
  .feyre-tryon-trigger {
    padding: 12px 22px;
    border: 0;
    border-radius: 10px;
    background: #111;
    color: #fff;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
  }

  .feyre-tryon-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,.45);
    z-index: 999999;
    display: flex;
    justify-content: flex-end;
    align-items: center;
    padding: 20px;
    box-sizing: border-box;
  }

  .feyre-tryon-drawer {
    width: 100%;
    max-width: 470px;
    height: calc(100vh - 40px);
    max-height: 860px;
    background: #fff;
    position: relative;
    box-shadow: -12px 0 30px rgba(0,0,0,.14);
    overflow-y: auto;
    border-radius: 24px;
    padding: 22px 24px 24px;
    box-sizing: border-box;
  }

  .feyre-close-btn {
    position: absolute;
    top: 16px;
    right: 18px;
    border: 0;
    background: transparent;
    color: #888;
    font-size: 28px;
    line-height: 1;
    cursor: pointer;
  }

  .feyre-drawer-head,
  .feyre-result-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 20px;
  }

  .feyre-drawer-head h2,
  .feyre-result-title {
    margin: 0;
    font-size: 20px;
    font-weight: 700;
    color: #1f2937;
  }

  .feyre-tryon-counter {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: #eaf7ec;
    color: #2f7a3e;
    border-radius: 999px;
    padding: 9px 14px;
    font-size: 13px;
    font-weight: 600;
    white-space: nowrap;
  }

  .feyre-step-progress {
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 6px 0 24px;
  }

  .feyre-step-circle {
    width: 42px;
    height: 42px;
    border-radius: 50%;
    border: 2px solid #d1d5db;
    color: #9ca3af;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 20px;
    background: #fff;
  }

  .feyre-step-circle.active {
    background: #000;
    color: #fff;
    border-color: #000;
  }

  .feyre-step-line {
    width: 64px;
    height: 3px;
    background: #e5e7eb;
    margin: 0 10px;
    border-radius: 999px;
  }

  .feyre-step-title {
    text-align: center;
    font-size: 22px;
    line-height: 1.2;
    font-weight: 700;
    color: #1f2937;
    margin-bottom: 10px;
  }

  .feyre-step-subtitle {
    text-align: center;
    font-size: 14px;
    color: #95a0ae;
    line-height: 1.45;
    margin-bottom: 22px;
  }

  .feyre-product-preview-card,
  .feyre-review-card,
  .feyre-result-card {
    background: #f5f5f7;
    border-radius: 18px;
    padding: 14px;
    text-align: center;
    margin-bottom: 16px;
  }

  .feyre-product-preview-img,
  .feyre-review-image,
  .feyre-result-image {
    width: 100%;
    max-width: 220px;
    height: 320px;
    object-fit: contain;
    display: block;
    margin: 0 auto;
    border-radius: 12px;
  }

  .feyre-review-image,
  .feyre-result-image {
    max-width: 250px;
    height: 380px;
  }

  .feyre-main-btn {
    width: 100%;
    border: 0;
    border-radius: 16px;
    background: #000;
    color: #fff;
    padding: 17px 18px;
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    margin-bottom: 12px;
  }

  .feyre-main-btn:disabled {
    opacity: .65;
    cursor: not-allowed;
  }

  .feyre-secondary-btn-lite {
    width: 100%;
    border: 1px solid #d7dce3;
    border-radius: 16px;
    background: #fff;
    color: #4b5563;
    padding: 17px 18px;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    margin-bottom: 14px;
  }

  .feyre-change-photo-btn {
    margin-top: 14px;
    border: 0;
    background: #fff;
    border-radius: 999px;
    padding: 11px 20px;
    font-size: 14px;
    font-weight: 600;
    color: #4b5563;
    box-shadow: 0 2px 10px rgba(0,0,0,.08);
    cursor: pointer;
  }

  .feyre-footer-note {
    text-align: center;
    color: #9ca3af;
    font-size: 12px;
    line-height: 1.45;
    margin-top: 6px;
  }

  .feyre-footer-note a {
    color: #6d5dd3;
    text-decoration: underline;
  }

  .feyre-powered {
    text-align: center;
    color: #c0c5ce;
    font-size: 13px;
    margin-top: 14px;
  }

  .feyre-powered span {
    color: #6d5dd3;
    font-weight: 700;
  }

  .feyre-loading-card {
    display: flex;
    justify-content: center;
    margin-bottom: 18px;
  }

  .feyre-loading-frame {
    position: relative;
    width: 230px;
    height: 320px;
    border-radius: 22px;
    overflow: hidden;
    background: #111;
  }

  .feyre-loading-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .feyre-scan-line {
    position: absolute;
    left: 8%;
    right: 8%;
    top: 0;
    height: 3px;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,.98), transparent);
    box-shadow: 0 0 20px rgba(255,255,255,.9);
    z-index: 2;
    animation: feyreScan 2s linear infinite;
    will-change: transform, top;
  }

  @keyframes feyreScan {
    0% { top: 8%; opacity: .9; }
    50% { top: 84%; opacity: 1; }
    100% { top: 8%; opacity: .9; }
  }

  .feyre-progress-wrap {
    max-width: 330px;
    margin: 0 auto 16px;
    text-align: center;
  }

  .feyre-progress-bar {
    height: 10px;
    background: #dee2e8;
    border-radius: 999px;
    overflow: hidden;
    margin-bottom: 12px;
  }

  .feyre-progress-fill {
    width: 12%;
    height: 100%;
    background: #111;
    border-radius: 999px;
    transition: width .6s ease;
  }

  .feyre-progress-label {
    color: #a0a7b3;
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 6px;
  }

  .feyre-progress-status {
    color: #374151;
    font-size: 15px;
    font-weight: 700;
  }

  .feyre-loading-note {
    max-width: 330px;
    margin: 18px auto 0;
    border: 1px solid #e6e8ee;
    background: #fbfbfc;
    color: #7b8494;
    border-radius: 16px;
    padding: 14px 16px;
    text-align: center;
    font-size: 13px;
    line-height: 1.45;
  }

  .loading-powered {
    margin-top: 22px;
  }

  .feyre-back-btn {
    border: 0;
    background: transparent;
    font-size: 34px;
    line-height: 1;
    color: #4b5563;
    cursor: pointer;
    padding: 0;
  }

  .result-cart-btn {
    margin-top: 16px;
  }

  .feyre-result-actions {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    margin-top: 10px;
  }

  .feyre-action-btn {
    border: 1px solid #d7dce3;
    background: #fff;
    color: #6b7280;
    border-radius: 14px;
    padding: 14px 10px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
  }

  .feyre-error-box {
    margin-top: 16px;
    background: #fff1f2;
    border: 1px solid #fecdd3;
    border-radius: 16px;
    padding: 14px;
  }

  .feyre-error-title {
    color: #9f1239;
    font-size: 15px;
    font-weight: 700;
    margin-bottom: 8px;
  }

  .feyre-error-box pre {
    margin: 0;
    white-space: pre-wrap;
    word-break: break-word;
    font-size: 12px;
    color: #881337;
  }

  @media (max-width: 640px) {
    .feyre-tryon-overlay {
      padding: 10px;
    }

    .feyre-tryon-drawer {
      max-width: 100%;
      height: calc(100vh - 20px);
      border-radius: 20px;
      padding: 18px 16px 20px;
    }

    .feyre-result-actions {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  let feyreProgressInterval = null;
  let feyreTryOnCount = 0;
  let feyreSelectedFile = null;

  function feyreOpenTryOnDrawer() {
    document.getElementById('feyre-tryon-overlay').style.display = 'flex';
    feyreShowStep(1);
    feyreHideError();
  }

  function feyreCloseTryOnDrawer() {
    document.getElementById('feyre-tryon-overlay').style.display = 'none';
    feyreStopProgress();
  }

  function feyreOverlayClose(event) {
    if (event.target.id === 'feyre-tryon-overlay') {
      feyreCloseTryOnDrawer();
    }
  }

  function feyreShowStep(step) {
    document.getElementById('feyre-step-1').style.display = step === 1 ? 'block' : 'none';
    document.getElementById('feyre-step-2').style.display = step === 2 ? 'block' : 'none';
    document.getElementById('feyre-step-3').style.display = step === 3 ? 'block' : 'none';
    document.getElementById('feyre-step-4').style.display = step === 4 ? 'block' : 'none';
  }

  function feyreHideError() {
    document.getElementById('feyre-error-box').style.display = 'none';
    document.getElementById('feyre-error-text').textContent = '';
  }

  function feyreShowError(message) {
    document.getElementById('feyre-error-box').style.display = 'block';
    document.getElementById('feyre-error-text').textContent = message || 'Unknown error';
  }

  function feyreUpdateCounter() {
    var text = feyreTryOnCount + ' Try-On';
    if (feyreTryOnCount !== 1) text += 's';
    document.getElementById('feyre-counter-text').textContent = text;
    var finalCounter = document.getElementById('feyre-counter-text-final');
    if (finalCounter) finalCounter.textContent = text;
  }

  function feyreHandlePhotoSelect(event) {
    var file = event.target.files && event.target.files[0];
    if (!file) return;

    feyreSelectedFile = file;
    feyreHideError();

    var reader = new FileReader();
    reader.onload = function(e) {
      document.getElementById('feyre-review-image').src = e.target.result;
      document.getElementById('feyre-loading-image').src = e.target.result;
      feyreShowStep(2);
    };
    reader.readAsDataURL(file);
  }

  function feyreStartProgress() {
    var fill = document.getElementById('feyre-progress-fill');
    var label = document.getElementById('feyre-progress-label');
    var status = document.getElementById('feyre-progress-status');
    var note = document.getElementById('feyre-loading-note');

    var steps = [
      { progress: 12, label: '12% Complete', status: 'Analyzing Your Photo....', note: 'Our AI adjusts for fabric stretch and drape on your body.' },
      { progress: 28, label: '28% Complete', status: 'Mapping your body shape....', note: 'Balancing silhouette and garment structure.' },
      { progress: 46, label: '46% Complete', status: 'Matching garment details....', note: 'Preparing the outfit fit on your frame.' },
      { progress: 67, label: '67% Complete', status: 'Refining folds and drape....', note: 'Improving texture and garment fall.' },
      { progress: 84, label: '84% Complete', status: 'Rendering your final fit....', note: 'Almost ready.' }
    ];

    let index = 0;
    fill.style.width = steps[0].progress + '%';
    label.textContent = steps[0].label;
    status.textContent = steps[0].status;
    note.textContent = steps[0].note;

    feyreStopProgress();
    feyreProgressInterval = setInterval(function() {
      index++;
      if (index >= steps.length) {
        clearInterval(feyreProgressInterval);
        feyreProgressInterval = null;
        return;
      }
      fill.style.width = steps[index].progress + '%';
      label.textContent = steps[index].label;
      status.textContent = steps[index].status;
      note.textContent = steps[index].note;
    }, 1400);
  }

  function feyreStopProgress() {
    if (feyreProgressInterval) {
      clearInterval(feyreProgressInterval);
      feyreProgressInterval = null;
    }
  }

  function feyreBackToReview() {
    feyreShowStep(2);
    feyreHideError();
  }

  function feyreDownloadResult() {
    var img = document.getElementById('feyre-result-image');
    if (!img || !img.src) return;
    var a = document.createElement('a');
    a.href = img.src;
    a.download = 'feyre-tryon-result.png';
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
  }

  async function feyreGenerateTryOn() {
    if (!feyreSelectedFile) {
      feyreShowError('Please choose a photo first.');
      return;
    }

    feyreHideError();
    feyreShowStep(3);
    feyreStartProgress();

    var formData = new FormData();
    var productImage = {{ product.featured_image | image_url: width: 800 | json }};

    formData.append('userImage', feyreSelectedFile);
    formData.append('productImage', productImage);

    try {
      var response = await fetch('/apps/tryon', {
        method: 'POST',
        body: formData
      });

      var text = await response.text();
      var data;

      try {
        data = JSON.parse(text);
      } catch (e) {
        throw new Error(text);
      }

      if (!response.ok || !data.success) {
        throw new Error(data.error || 'Generation failed');
      }

      feyreStopProgress();
      document.getElementById('feyre-progress-fill').style.width = '100%';
      document.getElementById('feyre-progress-label').textContent = '100% Complete';
      document.getElementById('feyre-progress-status').textContent = 'Preview ready';

      document.getElementById('feyre-result-image').src = data.resultImage;
      feyreTryOnCount += 1;
      feyreUpdateCounter();

      setTimeout(function() {
        feyreShowStep(4);
      }, 600);
    } catch (error) {
      feyreStopProgress();
      feyreShowStep(2);
      feyreShowError(error.message || 'Unknown error');
    }
  }
</script>

{% schema %}
{
  "name": "Try On Button",
  "target": "section",
  "settings": []
}
{% endschema %}